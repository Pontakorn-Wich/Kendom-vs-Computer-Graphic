// fragment.glsl – Fragment shader for coloring points
precision mediump float;
void main() {
    // Set each point's fragment color (RGBA):
    gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);  // red color
}
